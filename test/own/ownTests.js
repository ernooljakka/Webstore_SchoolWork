const user = require('../../models/user');
const db = require('../../models/db');

describe('checkpassword()', () => {
    it('Should return true if the password and the hash match', async () => {
    });
});

describe('userSchema', () => {
    it('Should return the created user', async () => {
    });
});

describe('getDburl()', () => {
    it('Should return Mongo database url', async () => {
    });
});

describe('disconnectDb()', () => {
    it('Should disconnect the application from Mongo database', async () => {
    });
});

describe('getProductCountFromCart()', () => {
    it('Should return product matching to the productId', async () => {
    });
});

describe('getAllProductsFromCart()', () => {
    it('Should return all products inside the shopping cart', async () => {
    });
});

describe('clearCart()', () => {
    it('Should clear the cart when clearCart() is called', async () => {
    });
});


describe('getCredentials()', () => {
    it('Should return null when the request is empty', async () => {
    });
});

describe('isJson()', () => {
    it('Should return true when requests content type is JSON', async () => {
    });
});

describe('noContent()', () => {
    it('should set response statuscode to 204', async () => {
    });
});
